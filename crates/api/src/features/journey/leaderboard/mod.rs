//! Leaderboards — the histories read across everybody, not down one person.
//!
//! The one journey surface that counts people it must not name. It answers from
//! a snapshot rather than the history, because a ranking cannot be narrowed to
//! the twenty rows it shows.

pub mod repository;
pub mod service;
pub mod types;

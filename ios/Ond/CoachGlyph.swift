/// The speech bubble that stands for the coach, written once — four surfaces
/// across three features draw it. At `ios/Ond/` rather than `OndUI`: it is
/// this app's name for one of its own destinations, and the design system
/// knows nothing about a coach. Symbols shared with the watch stay literals
/// on both sides — one string does not earn a package file.
enum CoachGlyph {
    static let symbol = "bubble.left"
}
